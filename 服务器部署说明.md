# 服务器部署说明

> 本文件用于云服务器从 GitHub 拉取代码后的更新部署，**每次拉取新代码后请务必阅读本文件**，确认是否有新的依赖或数据库迁移。

## 本次更新内容（2026-08-07）

### 新增功能
1. **用户认证系统**：新增完整的用户注册/登录/权限管理（基于 JWT + bcrypt）
2. **教师端 / 学生端区分**：按角色显示不同导航菜单和页面内容
3. **学生自助注册**：学生可通过注册页填写姓名+学号自助注册
4. **教师批量导入学生**：教师端「系统管理 → 学生管理」支持 Excel 批量导入
5. **个人中心**：教师和学生都可编辑姓名、选择头像图标、修改密码
6. **强制改密流程**：批量导入的学生首次登录强制修改密码
7. **AI 对话绑定学生**：AI 助教对话记录保存到对应学生名下

### 默认账号
- **教师账号**：`teacher` / `123456`（后端首次启动自动创建）
- **学生账号**：学生自助注册，或教师批量导入（初始密码 `123456`）

---

## ⚠️ 服务器部署必须做的事（重要！）

### 1. 安装新依赖
后端新增了用户认证相关依赖，**必须安装**，否则后端无法启动：

```bash
cd backend
pip install bcrypt PyJWT
```

或通过 requirements.txt 安装：
```bash
pip install -r requirements.txt
```

### 2. 重启后端
重启后端服务时，会自动完成以下操作（无需手动建表）：
- 自动创建 `users` 表（用户表）
- 自动创建 `chat_histories` 表（AI 对话历史表）
- 自动给已有的 `users` 表添加 `avatar` 列（兼容旧数据库）
- 自动初始化默认教师账号 `teacher / 123456`

```bash
cd backend
# 停止旧服务后，重新启动
nohup python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload > nohup.out 2>&1 &
```

### 3. 前端打包
前端新增了 `xlsx` 库（用于 Excel 批量导入），**必须重新打包**：

```bash
cd frontend
npm install        # 安装新依赖 xlsx
npm run build      # 重新打包
```

### 4. 验证部署
- 访问登录页，确认显示「登录」和「学生注册」入口
- 用 `teacher / 123456` 登录，确认能进入首页
- 教师进入「系统管理 → 学生管理」，确认页面正常加载

---

## 教师使用流程

1. 用 `teacher / 123456` 登录
2. 进入「系统管理 → 学生管理」
3. 点击「下载导入模板」获取 Excel 模板
4. 按模板填写学生名单（姓名、学号、班级、小组）
5. 上传 Excel 批量导入学生
6. 学生用姓名 + `123456` 登录，首次登录强制修改密码

## 学生使用流程

1. 方式一：自助注册（登录页点击「学生注册」）
2. 方式二：教师批量导入后，用姓名 + `123456` 登录
3. 首次登录强制修改密码
4. 可在「个人中心」编辑姓名、选择头像图标

---

## 技术架构说明

| 模块 | 技术方案 |
|---|---|
| 密码哈希 | bcrypt |
| 身份认证 | JWT（有效期 7 天） |
| 用户表 | users（id, username, password_hash, role, student_no, class_name, group_no, avatar, must_change_password） |
| AI 对话历史 | chat_histories（id, user_id, role, content, created_at） |
| 角色权限 | 路由 meta.roles + 路由守卫 + 后端 require_teacher 依赖 |

## 接口列表

| 接口 | 方法 | 说明 | 权限 |
|---|---|---|---|
| `/api/auth/register` | POST | 学生自助注册 | 公开 |
| `/api/auth/login` | POST | 登录（支持姓名/学号） | 公开 |
| `/api/auth/me` | GET | 获取当前用户信息 | 需登录 |
| `/api/auth/profile` | PUT | 更新个人资料 | 需登录 |
| `/api/auth/change-password` | POST | 修改密码 | 需登录 |
| `/api/auth/batch-register` | POST | 批量导入学生 | 仅教师 |
| `/api/auth/students` | GET | 学生列表 | 仅教师 |
| `/api/auth/reset-password/{id}` | POST | 重置学生密码 | 仅教师 |
| `/api/auth/students/{id}` | DELETE | 删除学生 | 仅教师 |
| `/api/auth/stats` | GET | 教师端统计 | 仅教师 |
