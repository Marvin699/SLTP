# 蚁群算法（ACO）核心公式说明

## 一、算法概述

蚁群算法（Ant Colony Optimization，简称 ACO）是一种模仿蚂蚁觅食行为的启发式优化算法，通过模拟蚂蚁在路径上释放信息素、并根据信息素浓度选择路径的机制，寻找最优解。

本系统采用蚁群算法解决**多无人机路径规划问题**，目标是在满足载重、航程约束的前提下，使所有无人机的总配送距离最短。

***

## 二、核心公式

### 公式 1：状态转移概率（选择下一个需求点）

蚂蚁在当前节点 `i` 选择下一个节点 `j` 的概率：

$$P(j) = \tau\_{ij}^{\alpha} \times \eta\_{ij}^{\beta}$$

**参数说明：**

| 符号           | 含义      | 说明                |
| ------------ | ------- | ----------------- |
| $\tau\_{ij}$ | 信息素浓度   | 路径上积累的"经验值"       |
| $\alpha$     | 信息素重要度  | 默认值 1.0，控制经验的权重   |
| $\eta\_{ij}$ | 启发式函数值  | 基于距离、优先级等计算的"吸引力" |
| $\beta$      | 启发函数重要度 | 默认值 5.0，控制贪心倾向的权重 |

> 💡 **直观理解**：蚂蚁倾向选择信息素浓度高（其他蚂蚁走过）且启发值大（更近、更重要）的路径。

***

### 公式 2：启发式函数（多因子吸引力）

启发式函数综合考虑多个因素，计算节点 `j` 对蚂蚁的吸引力：

$$\eta = 0.4 \times \eta\_{dist} + 0.3 \times \eta\_{priority} + 0.15 \times \eta\_{load} + 0.15 \times \eta\_{range}$$

**各因子计算方式：**

| 因子                       | 公式                              | 权重   | 含义          |
| ------------------------ | ------------------------------- | ---- | ----------- |
| 距离因子 $\eta\_{dist}$      | $1 / distance$                  | 0.4  | 距离越近，吸引力越大  |
| 优先级因子 $\eta\_{priority}$ | $priority\_weight / 5.0$        | 0.3  | 优先级越高，吸引力越大 |
| 载重因子 $\eta\_{load}$      | $1 - load\_ratio$               | 0.15 | 载重越轻，越灵活    |
| 航程因子 $\eta\_{range}$     | $remaining\_range / max\_range$ | 0.15 | 剩余航程越充裕，越安全 |

**优先级权重映射：**

| 优先级        | 权重值 |
| ---------- | --- |
| 紧急（urgent） | 5.0 |
| 高（high）    | 3.0 |
| 中（medium）  | 1.0 |
| 低（low）     | 0.5 |
| 普通（normal） | 0.3 |

> 💡 **直观理解**：蚂蚁优先选择距离近、优先级高、自身载重轻、航程充裕的需求点。

***

### 公式 3：信息素挥发

每轮迭代后，所有路径上的信息素按比例衰减：

$$\tau\_{ij} = (1 - \rho) \times \tau\_{ij}$$

**参数说明：**

| 符号     | 含义  | 默认值 |
| ------ | --- | --- |
| $\rho$ | 挥发率 | 0.5 |

> 💡 **直观理解**：旧的"经验"会逐渐消散，避免算法陷入局部最优。

***

### 公式 4：信息素沉积（普通蚂蚁）

蚂蚁完成一次路径后，在经过的路径上沉积信息素：

$$\Delta\tau\_{ij} = \frac{Q}{cost}$$

$$\tau\_{ij} = \tau\_{ij} + \Delta\tau\_{ij}$$

**参数说明：**

| 符号     | 含义        | 默认值  |
| ------ | --------- | ---- |
| $Q$    | 信息素强度常数   | 100  |
| $cost$ | 路径成本（总距离） | 动态计算 |

> 💡 **直观理解**：路径越短（成本越低），沉积的信息素越多，鼓励其他蚂蚁选择短路径。

***

### 公式 5：精英蚂蚁强化

对本轮最优路径进行额外强化，放大信息素沉积量：

$$\Delta\tau\_{elite} = \frac{Q}{cost} \times elite\_bonus$$

**参数说明：**

| 符号             | 含义     | 默认值                  |
| -------------- | ------ | -------------------- |
| $elite\_bonus$ | 精英强化倍数 | 2.0（最优蚂蚁）/ 1.5（其他精英） |

> 💡 **直观理解**：表现最好的蚂蚁留下更明显的"标记"，引导其他蚂蚁向最优解收敛。

***

## 三、算法流程

```
开始
  │
  ▼
初始化：信息素矩阵、参数设置
  │
  ▼
┌─────────────────────────┐
│     对每只蚂蚁循环        │
│  ┌───────────────────┐  │
│  │ 1. 选择下一个节点  │  │
│  │    P = τ^α × η^β  │  │
│  └───────────────────┘  │
│  ┌───────────────────┐  │
│  │ 2. 检查约束条件    │  │
│  │    - 载重限制      │  │
│  │    - 航程限制      │  │
│  └───────────────────┘  │
│  ┌───────────────────┐  │
│  │ 3. 生成完整路径    │  │
│  │    直到所有点访问   │  │
│  └───────────────────┘  │
│  ┌───────────────────┐  │
│  │ 4. 计算路径成本    │  │
│  └───────────────────┘  │
└─────────────────────────┘
  │
  ▼
信息素挥发：τ = (1-ρ) × τ
  │
  ▼
普通蚂蚁沉积：Δτ = Q / cost
  │
  ▼
精英蚂蚁强化：Δτ = (Q / cost) × bonus
  │
  ▼
是否达到最大迭代次数？
  ├── 是 → 输出最优解
  └── 否 → 回到"对每只蚂蚁循环"
```

***

## 四、参数配置说明

| 参数      | 符号              | 默认值 | 调优建议                          |
| ------- | --------------- | --- | ----------------------------- |
| 蚂蚁数量    | num\_ants       | 30  | 需求点 > 15 时增至 50，> 25 时增至 80   |
| 最大迭代次数  | max\_iterations | 100 | 需求点 > 15 时增至 150，> 25 时增至 200 |
| 信息素重要度  | α               | 1.0 | 值越大越依赖历史经验，建议范围 0.5\~2.0      |
| 启发函数重要度 | β               | 5.0 | 值越大越贪心，建议范围 2.0\~10.0         |
| 挥发率     | ρ               | 0.5 | 值越大收敛越快但易陷入局部最优，建议 0.3\~0.7   |
| 信息素强度   | Q               | 100 | 影响信息素沉积量，建议 50\~200           |
| 精英蚂蚁数   | elite\_ants     | 3   | 额外强化的精英数量，建议 2\~5             |

***

## 五、约束条件

本系统在求解过程中考虑以下实际约束：

1. **载重约束**：无人机装载的物资重量不得超过最大载重
2. **航程约束**：无人机必须能在剩余航程内到达下一个需求点并返回基地
3. **能耗约束**：无人机的电池能量足以完成当前任务

***

